/*var pc = document.getElementById("pro");
var searc = document.getElementById("search");
var pl = pc.querySelector("div");

search.addEventListener("keyup", function(event) {
    var ev = event.target.value.toUpperCase();

    for (let count = 0; count < pl.length; count++) {
        var pn = pl[count].querySelector("p").textContent;
        if (pn.toUpperCase().indexOf(ev) < 0) {
            pl[count].style.display = "none";
        } else {
            pl[count].style.display = "block";
        }
    }
});*/

